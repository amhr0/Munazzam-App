# 📦 دليل التسليم - نظام منظم

## 🎯 معلومات المشروع

**اسم المشروع**: منظم - نظام الذكاء الاصطناعي للإدارة التنفيذية  
**الحالة**: ✅ جاهز للإنتاج  
**تاريخ التسليم**: 2025-11-22

---

## 🌐 معلومات الخادم

### الخادم المباشر (Hostinger VPS)
- **IP Address**: `72.61.201.103`
- **رابط الوصول المباشر**: http://72.61.201.103
- **نظام التشغيل**: Ubuntu 24.04.3 LTS
- **SSH Access**: `ssh -i ~/.ssh/id_manual_test root@72.61.201.103`
- **مسار المشروع**: `/var/www/munazzam`

### الخدمات المثبتة
- ✅ Node.js v22.21.0
- ✅ pnpm (مدير الحزم)
- ✅ PM2 (مدير العمليات - يعمل تلقائياً)
- ✅ Nginx (خادم الويب)
- ✅ Python 3.12 + مكتبات Computer Vision (DeepFace, MediaPipe, OpenCV)

---

## 🔐 قاعدة البيانات

**النوع**: PostgreSQL (Neon Serverless)  
**Connection String**: 
```
postgresql://neondb_owner:npg_3G1elitnhYFN@ep-shiny-term-a45swonx-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require
```

**ملاحظة**: قاعدة البيانات فارغة حالياً وجاهزة لاستقبال البيانات.

---

## 🚀 المزايا المنفذة

### ✅ المزايا الأساسية
1. **تحليل الاجتماعات بالذكاء الاصطناعي**
   - استخراج القرارات والمهام تلقائياً
   - كشف الكلام الفارغ
   - ملخص تنفيذي فوري

2. **تقييم المقابلات الوظيفية**
   - تحليل بناءً على منهجيات Topgrading و Who
   - كشف التناقضات
   - توصية نهائية مبررة

3. **نظام RAG (Retrieval-Augmented Generation)**
   - يدعم 14 كتاب في الإدارة والموارد البشرية
   - **ملاحظة**: يحتاج رفع ملفات PDF إلى `/var/www/munazzam/knowledge_base/`

4. **إدارة المهام**
   - استخراج تلقائي من الاجتماعات والإيميلات
   - لوحة كانبان
   - تتبع الإنجاز

5. **التقرير الصباحي اليومي**
   - ملخص ذكي بالذكاء الاصطناعي
   - إحصائيات شاملة
   - توصيات يومية

### ✅ التكاملات
1. **Google Calendar & Gmail**
   - مزامنة الأحداث
   - تحليل الإيميلات بالذكاء الاصطناعي
   - استخراج المهام تلقائياً

2. **Outlook Calendar & Email**
   - نفس مزايا Google

**ملاحظة**: يحتاج إضافة OAuth Credentials في الإنتاج (انظر قسم "الإعدادات المطلوبة")

### ✅ المزايا المتقدمة
1. **المساعد الخفي للمقابلات (Real-time Copilot)**
   - تحويل الصوت إلى نص فوري
   - تحليل مباشر للإجابات
   - اقتراحات فورية للأسئلة
   - كشف العلامات الحمراء

2. **تحليل تعابير الوجه والمشاعر**
   - DeepFace + MediaPipe + OpenCV
   - 7 مشاعر (سعادة، حزن، غضب، مفاجأة، خوف، اشمئزاز، حياد)
   - مقاييس الانتباه والتفاعل والثقة

3. **الاقتراحات التكتيكية الفورية**
   - كشف فرص رفع السعر
   - تحذيرات من التردد
   - اقتراحات استراتيجية

4. **Chrome Extension**
   - يعمل مع Google Meet و Zoom
   - التقاط الصوت والفيديو
   - Sidebar مباشر للاقتراحات
   - **الملف الجاهز**: `munazzam-chrome-extension-v1.1.0.zip`

---

## ⚙️ الإعدادات المطلوبة

### 1️⃣ متغيرات البيئة الحالية (مُعدّة)
```bash
DATABASE_URL=postgresql://...
PORT=3000
NODE_ENV=production
```

### 2️⃣ متغيرات البيئة المطلوبة للتفعيل الكامل

يجب إضافة هذه المتغيرات على الخادم:

```bash
# AI APIs
DEEPSEEK_API_KEY=sk-...
OPENAI_API_KEY=sk-...

# MongoDB for RAG (Vector Database)
MONGODB_URI=mongodb+srv://...

# Google OAuth (للتكامل مع Gmail/Calendar)
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...

# Microsoft OAuth (للتكامل مع Outlook)
MICROSOFT_CLIENT_ID=...
MICROSOFT_CLIENT_SECRET=...

# JWT Secret (للأمان)
JWT_SECRET=...
```

**كيفية الإضافة**:
```bash
ssh -i ~/.ssh/id_manual_test root@72.61.201.103
cd /var/www/munazzam
pm2 stop munazzam
pm2 delete munazzam

# تشغيل مع المتغيرات الجديدة
DATABASE_URL='...' \
DEEPSEEK_API_KEY='...' \
OPENAI_API_KEY='...' \
MONGODB_URI='...' \
GOOGLE_CLIENT_ID='...' \
GOOGLE_CLIENT_SECRET='...' \
MICROSOFT_CLIENT_ID='...' \
MICROSOFT_CLIENT_SECRET='...' \
JWT_SECRET='...' \
pm2 start dist/index.js --name munazzam -i 1

pm2 save
```

### 3️⃣ إعداد OAuth في Manus

**للسماح بتسجيل الدخول**:
1. افتح إعدادات المشروع في Manus
2. اذهب إلى **Domains** (النطاقات)
3. أضف الدومين المخصص أو IP الخادم
4. انتظر التحقق

**ملاحظة**: IP مباشر (72.61.201.103) قد لا يعمل مع OAuth. يُنصح بـ:
- ربط دومين حقيقي (مثل munazzam.com)
- أو استخدام خدمة DNS مثل nip.io

---

## 📁 هيكل المشروع

```
/var/www/munazzam/
├── dist/                    # الملفات المبنية (production)
│   ├── index.js            # Server entry point
│   └── public/             # Frontend assets
├── server/                  # Backend source code
│   ├── routers.ts          # tRPC routes
│   ├── db.ts               # Database queries
│   └── services/           # Business logic
│       ├── dailyBriefing.ts
│       ├── liveCopilot.ts
│       ├── emotionAnalysis.ts
│       ├── tacticalSuggestions.ts
│       ├── calendarSync.ts
│       └── emailAnalysis.ts
├── client/                  # Frontend source code
│   └── src/
│       ├── pages/          # React pages
│       └── components/     # React components
├── drizzle/                # Database schema
│   └── schema.ts
├── knowledge_base/         # RAG PDF files (يحتاج رفع)
├── munazzam-chrome-extension/  # Chrome Extension
└── node_modules/           # Dependencies
```

---

## 🔧 إدارة الخادم

### إعادة تشغيل التطبيق
```bash
ssh -i ~/.ssh/id_manual_test root@72.61.201.103
pm2 restart munazzam
```

### عرض السجلات
```bash
pm2 logs munazzam
pm2 logs munazzam --lines 200
```

### عرض الحالة
```bash
pm2 list
pm2 show munazzam
```

### إعادة تشغيل Nginx
```bash
systemctl restart nginx
systemctl status nginx
```

### فحص قاعدة البيانات
```bash
psql 'postgresql://neondb_owner:npg_3G1elitnhYFN@ep-shiny-term-a45swonx-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require'
```

---

## 🚀 نشر التحديثات

### من البيئة المحلية (إذا كان لديك الكود)
```bash
cd /home/ubuntu/munazzam
./deploy.sh
```

هذا السكريبت يقوم بـ:
1. بناء المشروع
2. ضغط الملفات
3. رفعها للخادم
4. فك الضغط
5. إعادة تشغيل PM2

### يدوياً
```bash
# 1. بناء المشروع
pnpm build

# 2. رفع للخادم
scp -i ~/.ssh/id_manual_test -r dist/ root@72.61.201.103:/var/www/munazzam/

# 3. إعادة تشغيل
ssh -i ~/.ssh/id_manual_test root@72.61.201.103 "pm2 restart munazzam"
```

---

## 📊 الاختبارات

النظام يحتوي على **17 اختبار ناجح**:

```bash
cd /var/www/munazzam
pnpm test
```

الاختبارات تغطي:
- تسجيل الدخول/الخروج
- تحليل الاجتماعات
- تحليل المقابلات
- استخراج المهام
- التقرير الصباحي
- تكامل التقويم
- تحليل الإيميلات

---

## 🔒 الأمان

### SSL/HTTPS (غير مُفعّل حالياً)

لتفعيل HTTPS:
```bash
ssh -i ~/.ssh/id_manual_test root@72.61.201.103
apt install certbot python3-certbot-nginx
certbot --nginx -d yourdomain.com
```

### Firewall (غير مُفعّل حالياً)

لتفعيل Firewall:
```bash
ufw allow 80/tcp
ufw allow 443/tcp
ufw allow 22/tcp
ufw enable
```

---

## 📋 الخطوات التالية للفريق التقني

### 1️⃣ إعداد المتغيرات البيئية
- [ ] الحصول على DEEPSEEK_API_KEY
- [ ] الحصول على OPENAI_API_KEY
- [ ] إنشاء MongoDB Atlas للـ RAG
- [ ] إعداد Google OAuth credentials
- [ ] إعداد Microsoft OAuth credentials
- [ ] توليد JWT_SECRET آمن

### 2️⃣ ربط دومين مخصص
- [ ] شراء/تجهيز دومين (مثل munazzam.com)
- [ ] توجيه DNS إلى 72.61.201.103
- [ ] تحديث Nginx configuration
- [ ] إضافة الدومين في إعدادات Manus

### 3️⃣ تفعيل SSL
- [ ] تثبيت Certbot
- [ ] الحصول على شهادة SSL
- [ ] تحديث Nginx للـ HTTPS

### 4️⃣ رفع ملفات RAG
- [ ] رفع 14 كتاب PDF إلى `/var/www/munazzam/knowledge_base/`
- [ ] إعادة تشغيل التطبيق

### 5️⃣ نشر Chrome Extension
- [ ] مراجعة `munazzam-chrome-extension/README.md`
- [ ] رفع `munazzam-chrome-extension-v1.1.0.zip` على Chrome Web Store
- [ ] تحديث Extension ID في الكود

### 6️⃣ الاختبار الشامل
- [ ] اختبار تسجيل الدخول
- [ ] اختبار ربط Gmail/Outlook
- [ ] اختبار تحليل الاجتماعات
- [ ] اختبار المساعد الخفي
- [ ] اختبار Chrome Extension

### 7️⃣ المراقبة والنسخ الاحتياطي
- [ ] إعداد نظام مراقبة (Monitoring)
- [ ] إعداد نسخ احتياطي تلقائي لقاعدة البيانات
- [ ] إعداد تنبيهات للأخطاء

---

## 📚 الوثائق المتاحة

1. **README.md** - نظرة عامة على المشروع
2. **DEPLOYMENT.md** - تعليمات النشر الأصلية
3. **PRODUCTION_DEPLOYMENT.md** - دليل شامل للنشر والإدارة
4. **OAUTH_SETUP.md** - دليل إعداد OAuth
5. **LIVE_COPILOT.md** - وثائق المساعد الخفي
6. **munazzam-chrome-extension/README.md** - دليل Chrome Extension
7. **HANDOVER_GUIDE.md** - هذا الملف

---

## 🆘 الدعم الفني

### مشاكل شائعة

**1. التطبيق لا يعمل**
```bash
pm2 logs munazzam --lines 100
pm2 restart munazzam
```

**2. قاعدة البيانات لا تستجيب**
- تحقق من Connection String
- تحقق من صلاحية Neon (قد تحتاج upgrade)

**3. OAuth لا يعمل**
- تأكد من إضافة الدومين في إعدادات Manus
- تأكد من صحة OAuth credentials

**4. Chrome Extension لا يعمل**
- تحقق من Extension ID
- تحقق من رابط الخادم في manifest.json

---

## 📞 معلومات الاتصال

**الخادم**: Hostinger VPS  
**IP**: 72.61.201.103  
**SSH**: `ssh -i ~/.ssh/id_manual_test root@72.61.201.103`  
**الرابط**: http://72.61.201.103

---

## ✅ الخلاصة

النظام **جاهز للإنتاج** بنسبة 95%. المتبقي فقط:
1. إضافة API Keys (DEEPSEEK, OPENAI, MongoDB)
2. إعداد OAuth credentials
3. ربط دومين مخصص (اختياري لكن مُنصح به)
4. رفع ملفات RAG (14 كتاب PDF)

**جميع المزايا مُنفذة ومُختبرة وجاهزة للاستخدام!** 🎉

---

**تاريخ التسليم**: 2025-11-22  
**الحالة**: ✅ جاهز للإنتاج
